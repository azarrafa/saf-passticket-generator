apply plugin: 'java'
compileJava.options.fork = true
compileJava.options.forkOptions.executable ='/Users/az892631/Library/Java/JavaVirtualMachines/corretto-1.8.0_312/Contents/Home/bin/javac'
if(JavaVersion.current() >= JavaVersion.VERSION_11) {
    dependencies {
        implementation 'javax.xml.bind:jaxb-api:2.3.0'
        implementation 'com.sun.xml.bind:jaxb-core:2.3.0'
        implementation 'com.sun.xml.bind:jaxb-impl:2.3.0'
    }
}

repositories {
    jcenter()
}

dependencies {
    testImplementation 'junit:junit:4.12'
}

task(run, dependsOn: jar, type: JavaExec ) {
    main = 'racfPassTicket.example.Main'
    classpath = sourceSets.main.runtimeClasspath
}
jar {
    manifest {
        attributes(

                'Main-Class': 'racfPassTicket.example.Main'
        )
    }
}

